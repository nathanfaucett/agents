# Github Actions

Recursively download all references from https://docs.github.com/en/actions/reference, then convert them into Markdown files and place in `references/` update the SKILL.md with the outline so that this skill can use as guidance for GitHub Actions.